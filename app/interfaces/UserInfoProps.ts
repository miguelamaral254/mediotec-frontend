export interface UserInfoProps {
    user: {
      name: string;
      cpf: string;
      email: string;
      role: string;
      active: boolean;
      birthDate: string;
      phone: string;
      registration: string;
      address: string;
    };
  }